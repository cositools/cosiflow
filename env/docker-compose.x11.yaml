# Explicit opt-in for trusted tasks that require access to the host X server.
services:
  airflow:
    environment:
      DISPLAY: ${DISPLAY:?DISPLAY is required when the X11 override is enabled}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:ro
